# CSM Dev Kit.

This CMDLine application provides utilities for [CSM] organization developers, maintainers, etc. Handles regular tasks
related with development, database management, etc. 

It's free to open tickets and submit PR's to improve or request features. 

## Installation.

To install the CMDLine tools properly run:

dotnet tool install --global CSM.DevKit